#include <iostream>
#include <vector>
#include <map>
#include <string>
#include <map>
#include <algorithm>
#include <cmath>
using namespace std;
map<int, string> Hash;

int main()
{
    int fenshu[10] = {85, 46, 85, 60, 98, 75, 50, 87, 75, 30};
    for (int i = 0; i < 10; i++)
    {
        int a = fenshu[i];
        switch (a)
        {
        case a >= 90:
            break;
        case a >= 80:
            break;
        case a >= 70:
            break;
        case a >= 60:
            break;
        }
    }
}